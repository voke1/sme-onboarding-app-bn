import { Auth.MiddlewareMiddleware } from './auth.middleware.middleware';

describe('Auth.MiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new Auth.MiddlewareMiddleware()).toBeDefined();
  });
});
